[tox]
isolated_build = True
envlist = py310, py311

[gh-actions]
python =
    3.10: py310
    3.11: py311

[testenv]
whitelist_externals =
    poetry
require_locked_deps = true
install_dev_deps = true
extras =
    test
commands =
    poetry install -v
    poetry run mypy . --ignore-missing-imports
